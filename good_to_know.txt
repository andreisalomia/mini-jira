It is required that your private key files are NOT accessible by others.
This private key will be ignored.

Explorer -> File properties -> Security -> Advanced -> Delete permission entries -> Add just the user

ssh -i minijira-week2-key.pem ubuntu@<PUBLIC_IP>

De ce avem nevoie de NGINX?
-un singur port expus
-backend ascuns
-acelasi setup pentru dev/prod
-fara probleme de cors

Reverse proxy: hides MY servers

terraform - init - plan - apply - destroy - show